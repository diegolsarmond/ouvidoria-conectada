// Shim — supabase client substituído pelo backend Express.
// Mantido apenas para compatibilidade de importações residuais.
// Não use este módulo em código novo; use api-client.ts ou api.ts.

export const supabase = {} as any;
